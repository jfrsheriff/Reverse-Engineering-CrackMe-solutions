{
    IBClasses = (
        {
            ACTIONS = {checkRegister = id; endSheet = id; }; 
            CLASS = AppManager; 
            LANGUAGE = ObjC; 
            OUTLETS = {mainWindow = id; name = id; serial = id; successSheet = id; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}